[project]
name = "agentgate"
version = "0.1.0"
description = "Safety-first CLI and GitHub Action for checking AI-generated code changes before PRs."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "a78c7" }]

[project.scripts]
agentgate = "agentgate:main"

[tool.setuptools]
py-modules = ["agentgate"]

[tool.agentgate]
version = "0.1.0"
